# Dev (recomendado): proxy do Vite — funciona com API em HTTP ou HTTPS
VITE_API_URL=/api

# Alternativa: ligação directa (usa https:// se tiveres certificados no servidor)
# VITE_API_URL=https://localhost:3001/api
# VITE_API_URL=http://localhost:3001/api
