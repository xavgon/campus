export const REMEMBER_EMAIL_KEY = 'campus_remember_email';

/** Credenciais de desenvolvimento — pré-preenchidas só em `import.meta.env.DEV`. */
export const DEFAULT_ADMIN_LOGIN = {
  email: 'admin@campus.co.ao',
  password: 'Campus123',
} as const;
